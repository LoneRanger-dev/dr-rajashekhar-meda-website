"use client";

/**
 * Clean static background container — 3D background animation is completely removed per client request.
 */
export function HeroVisual(_props?: { className?: string }) {
  return null;
}
