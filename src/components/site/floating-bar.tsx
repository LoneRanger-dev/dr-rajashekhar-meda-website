"use client";

export function FloatingBar() {
  // Completely removed floating sidebar buttons per user instruction.
  // Contact buttons (24/7 Emergency, WhatsApp, Book Appointment) are integrated inside the ChatWidget.
  return null;
}
